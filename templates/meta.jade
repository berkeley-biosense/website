
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + ' - '}

block header
  div.nav
    a(href=contents.index.url) &lt; BioSENSE
  h1= page.title

block content
 section.about
    !=contents['about.md'].html
  article.image-grid
    section.content!= typogr(page.html).typogrify()

block prepend footer
